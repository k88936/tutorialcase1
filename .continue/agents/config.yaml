name: Fullstack Tutorial Agent
version: 1.0.0
schema: v1

models:
  - name: "Course Agent"
    provider: openrouter
    model: anthropic/claude-sonnet-4.5
    apiKey: ${{ secrets.OPENROUTER_API_KEY }}
    roles:
      - chat
      - edit
      - apply
    capabilities:
      - tool_use