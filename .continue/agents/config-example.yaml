name: Fullstack Tutorial Agent
version: 1.0.0
schema: v1

models:
  - name: "Course Agent"
    provider: openai
# Replace MODEL_NAME, BASE_URL, and API_KEY here with your own ones.
    model: MODEL_NAME
    apiBase: BASE_URL
    apiKey: API_KEY 

    roles:
      - chat
      - edit
      - apply
    capabilities:
      - tool_use