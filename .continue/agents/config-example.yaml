name: Fullstack Tutorial Agent
version: 1.0.0
schema: v1

models:
  - name: "Course Agent"
    provider: openai
    model: MODEL_NAME
    apiBase: API_BASE_URL
    apiKey: API_KEY 
    roles:
      - chat
      - edit
      - apply
    capabilities:
      - tool_use